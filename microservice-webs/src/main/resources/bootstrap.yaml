spring:
  application:
    name: microservice-webs
  cloud:
    config:
      enabled: true
      uri: ${CONFIG_SERVER_URL:http://localhost:8081}
      profile: ${CONFIG_PROFILE:${SPRING_PROFILES_ACTIVE:default}}
      fail-fast: true


